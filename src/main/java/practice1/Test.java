package practice1;

public class Test {
    /*
  1. Reverse a string without using built-in reverse methods.
2. Find duplicates in an array in O(n) time/space using a HashSet .
3. Check if two strings are anagrams (sort or frequency-count approach).
4. First non-repeating character in a string.
5. Two Sum — given an array and target, return indices of the two numbers that add up to
target ( HashMap for O(n)).
6. Valid parentheses — check balanced brackets using a Stack .
7. FizzBuzz variant using Streams ( IntStream.rangeClosed ).
8. Merge two sorted arrays in place.
9. Print all pairs in an array with a given sum, avoiding duplicate pairs.
10. Given a List<Order> , use Streams to compute total revenue per customer, sorted
descending.
Practice writing these cleanly in a plain text editor (no autocomplete) — HirePro’s live editor
     */
}
